{
  "name": "bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "start": "bun --env-file=../../.env src/index.ts",
    "typecheck": "tsc --noEmit",
    "replay": "bun --env-file=../../.env scripts/replay.ts",
    "seed-demo": "bun --env-file=../../.env scripts/seed-demo.ts",
    "test:parser": "bun --env-file=../../.env scripts/test-parser.ts"
  },
  "dependencies": {
    "@autopilot/aomi-bridge": "workspace:*",
    "@autopilot/shared-types": "workspace:*",
    "@google/genai": "^1.9.0",
    "hono": "^4.6.15",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
